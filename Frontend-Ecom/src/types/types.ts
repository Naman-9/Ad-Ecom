export interface User {
  name: string;
  email: string;
  photo: string;
  gender: string;
  role: string;
  dob: string; // will come in string format
  _id: string;
}

export interface Product {
  name: string;
  category: string;
  photo: string;
  price: number;
  stock: number;
  _id: string;
}


